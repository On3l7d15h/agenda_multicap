//Función autollamada
(() => {

	addEventListener("load", () => {
			cont3.style.transform = `translateX(${0}px)`;
				cont3.style.opacity = 1;

			//divs internos
			cont1.style.transform = `translateY(${0}px)`;
			cont2.style.transform = `translateY(${0}px)`;

			//sidebar
      side.style.transform = `translateX(${0}px)`; 
	})

})()
